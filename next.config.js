/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["anvogue.vercel.app", "avatars.githubusercontent.com"],
  },
};

module.exports = nextConfig;
